*.pdf
!repo_docs/*.pdf
docs/
report_outline.md
__pycache__/
*.egg-info/

# run outputs (pose .h5/.json results, logs) and local dataset copies
runs/
data/
outputs/
.venv/

# Intel NEO OpenCL driver debug dump (only appears when NEOReadDebugKeys/
# PrintDebugSettings env vars are set)
igdrcl_dumped.config